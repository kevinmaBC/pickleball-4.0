# APP-V2 Phase 0 / S1 Handoff Package

Sprint: **S1 Canonical Runtime Integration**

Frozen upstream baseline: `be46174` (S0).

Start with `CLAUDE_S1_PROMPT.md`.

This package was prepared after GPT reviewed the actual frozen GitHub repository at S0 commit.
S1 is deliberately small: wire canonical data into the real browser/PWA runtime, make it
offline-aware, inventory legacy data dependencies, preserve existing behavior, then stop.

Do not start S2.
